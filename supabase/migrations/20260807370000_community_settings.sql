-- Intentionally empty. This version originally tried to create a
-- "community_settings" table, but that name was already taken by the
-- association-level settings row. The real table ships as
-- 20260807380000_community_onboarding.sql under a name that does not clash.
select 1;
